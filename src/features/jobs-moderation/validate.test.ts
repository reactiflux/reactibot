import { describe, expect, expectTypeOf, it } from "vitest";
// import { formatting, web3, participation } from "./validate";

describe.skip("parseContent", () => {
  describe("formatting", () => {
    // formatting();
  });
  describe("web3", () => {
    // web3();
  });
  describe("participation", () => {
    // participation();
  });
});
