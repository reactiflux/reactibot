name: Deploy

on:
  push:
    branches: master

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - run: |
        git clone http://runner@github.com/reactiflux/reactibot.git
        cd reactibot
        git push --quiet $PRODUCTION_GIT_URL
